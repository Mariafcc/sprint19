import React from "react"

const Header = () => {
    return (
        <div className="header">
            <jumbotron>
                <h1>Employee Directory</h1>
                <p>Click the name to filter or use the search bar to narrow results</p>
            </jumbotron>
        </div>
    )
}

export default Header;